# 协作规则

这是一个 Python 小说 agent 项目。所有修改都应服务于小说生成、流程编排、提示词调用与测试验证，不要偏离这个边界。

## 工作原则

1. 优先做小步、可验证的修改；一次只解决一个明确问题。
2. 默认先补测试，再扩展功能；若无法先写测试，需说明原因并保持改动最小。
3. 修改后优先运行最小相关 `pytest`，先验证受影响范围，再考虑扩大测试。
4. 不要一次改很多无关文件；除非明确要求，保持变更集中。
5. 业务模块尽量职责单一，避免把流程控制、数据处理、提示词拼接混在一起。
6. 报错信息要清晰，能直接说明失败位置、原因和必要上下文。

## 变更约束

1. 不要随意修改 `prompts/` 下的文本模板；只有在任务明确要求或已有测试覆盖时才调整。
2. 未经明确要求，不要引入新依赖；优先复用标准库和现有能力。
3. 不要擅自新增 UI 框架；这是 Python 小说 agent 项目，不要把任务扩展成前端重构。
4. 未经要求，不要顺手重命名、搬移或大规模格式化现有文件。

## 执行习惯

1. 开始前先定位相关模块与现有测试，避免凭猜测改代码。
2. 改动应尽量与已有结构一致，先复用现有模式，再考虑新增抽象。
3. 提交结果时说明改了什么、为什么改、运行了哪些最小验证。
